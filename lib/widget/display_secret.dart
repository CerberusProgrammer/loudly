import 'package:flutter/material.dart';

class DisplaySecret extends StatelessWidget {
  const DisplaySecret({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
